<% content_for :title, "Add Employee" %>

<div class="card border-0 shadow-sm mx-auto form-card-wide">
  <div class="card-body p-4 p-md-5">
    <h1 class="h3 fw-semibold mb-2">Add employee</h1>
    <p class="text-secondary mb-4">Add an employee for <%= @location.name %> and assign their positions.</p>

    <%= render "form" %>
  </div>
</div>
