<% content_for :title, "Edit Employee" %>

<div class="card border-0 shadow-sm mx-auto form-card-wide">
  <div class="card-body p-4 p-md-5">
    <h1 class="h3 fw-semibold mb-2">Edit employee</h1>
    <p class="text-secondary mb-4">Update employee details and position assignments for <%= @location.name %>.</p>

    <%= render "form" %>
  </div>
</div>
