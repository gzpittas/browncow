<% content_for :title, "Edit Location" %>

<div class="auth-card card border-0 shadow-sm mx-auto">
  <div class="card-body p-4 p-md-5">
    <h1 class="h3 fw-semibold mb-2">Edit location</h1>
    <p class="text-secondary mb-4">Keep this restaurant location accurate.</p>

    <%= render "form" %>
  </div>
</div>
