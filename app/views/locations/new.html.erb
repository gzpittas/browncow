<% content_for :title, "Add Location" %>

<div class="auth-card card border-0 shadow-sm mx-auto">
  <div class="card-body p-4 p-md-5">
    <h1 class="h3 fw-semibold mb-2">Add location</h1>
    <p class="text-secondary mb-4">Start with the restaurant or store where employees will work.</p>

    <%= render "form" %>
  </div>
</div>
