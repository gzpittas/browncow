<div class="print-position-schedule">
  <% @positions.each do |position| %>
    <% position_employees = @employees_by_position[position] || [] %>
    <section class="print-position-section" style="--print-position-color: <%= position.display_color %>;">
      <h2><%= position.name.pluralize %></h2>

      <% if position_employees.any? %>
        <table class="print-employee-table">
          <thead>
            <tr>
              <th>Employee</th>
              <% @week_dates.each do |date| %>
                <th><%= schedule_day_label(date) %></th>
              <% end %>
            </tr>
          </thead>
          <tbody>
            <% position_employees.each do |employee| %>
              <tr>
                <th><%= employee.display_name %></th>
                <% @week_dates.each do |date| %>
                  <% shifts = @shifts_by_employee_and_date[[ employee.id, date ]] || [] %>
                  <td>
                    <% if shifts.any? %>
                      <% shifts.each do |shift| %>
                        <div class="print-shift-line">
                          <strong><%= shift.position.name %></strong>
                          <span class="print-shift-time-stack">
                            <span><%= shift_time_range_lines(shift).first %></span>
                            <span><%= shift_time_range_lines(shift).last %></span>
                          </span>
                        </div>
                      <% end %>
                    <% end %>
                  </td>
                <% end %>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <p class="print-empty">No active employees are assigned to this position yet.</p>
      <% end %>
    </section>
  <% end %>
</div>
