<% content_for :title, "Add Shift" %>

<div class="card border-0 shadow-sm mx-auto form-card-wide">
  <div class="card-body p-4 p-md-5">
    <h1 class="h3 fw-semibold mb-2">Add shift</h1>
    <p class="text-secondary mb-4">Create one same-day shift for this weekly schedule.</p>

    <%= render "form" %>
  </div>
</div>
