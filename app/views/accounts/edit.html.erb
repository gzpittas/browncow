<% content_for :title, "Account" %>

<div class="auth-card card border-0 shadow-sm mx-auto">
  <div class="card-body p-4 p-md-5">
    <h1 class="h3 fw-semibold mb-2">Account</h1>
    <p class="text-secondary mb-4">Update the restaurant business details for this workspace.</p>

    <%= render "form" %>
  </div>
</div>
